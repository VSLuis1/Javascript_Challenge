// For of loop - used to iterate over the values in an iterable object (array, string, etc.)

let values = [1,2,3,4,5];

for (const val of values){
    console.log(val)
}

// example
let  fruits = ["Apple, Banana, Orange"];

for (const name of fruits){
    console.log(fruits)
}

// for of loop with a string
let user = "Luis";
for(const name of user){
    console.log(name);
}
